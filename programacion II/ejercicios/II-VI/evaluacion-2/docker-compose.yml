version: '3.8'

services:
  basededatos:
    image: mariadb:10.11
    container_name: bd_estudiantes
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: uptt_notas
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data:
